import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

const StyleWriter = ({children}:any) => {
    return (
            <Text>{children}</Text>
    )
}

export default StyleWriter

const styles = StyleSheet.create({})
